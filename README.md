# node-carpenter
Carpenter is a ful framework based around sequelize and react

# Server Initialization
    Typical initialization:
    ```

    ```
    
    
    async DatabaseInitialize(dbConfig) {
    async SeedCoreData(modelList, force=false) {
    async SeedDemoData(modelList, force=false) {
    Start() {
    async Stop() {
    

# Component Development
When initializing a new stack, the stack should have an initialization function that uses the following carpenter modules:
    AddModel(model) {
    AddRouteObject(route) {
    addJobTemplate(jobTemplate) {
    addPreactComponent(component, path) {
    addPublicPath(publicPath) {
   